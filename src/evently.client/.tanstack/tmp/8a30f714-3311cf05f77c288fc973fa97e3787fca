import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/gatherings/$gatheringId')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/gatherings/$gatheringId"!</div>
}
